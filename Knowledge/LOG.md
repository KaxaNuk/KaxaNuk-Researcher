# Log

> Append-only record of every `/compile`, `/audit` and `/refresh-index`, oldest first. Written by
> the researcher, never edited by hand. File-level actions on `Knowledge/` only — never query content,
> never answers, never any path under `Philosophy/Private/`. The last five entries:
> `grep "^## \[" Knowledge/LOG.md | tail -5`

---
