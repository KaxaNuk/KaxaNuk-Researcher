# Index

> The one index of everything in `Knowledge/`, grouped by domain, one line per note — a book's line
> links its `INDEX.md`, with one indented line per chapter read: a standard markdown link and the
> sentence that says what the note settles. Rebuilt by `read` and `refresh-index`; never edited by
> hand outside those runs. **Read this first, always** — a question is answered by walking the index
> and the links before opening any note.

*(empty — nothing read yet. Drop a source into `Sources/` and run `read`.)*
