# Index

> The one index of everything in `Knowledge/`, grouped by domain: **Concepts** first — one line per
> concept or synthesis page, its title and the sentence that defines it — then **Sources**, one line
> per note, a book's line linking its `INDEX.md` with one indented line per chapter read. Rebuilt by
> `read` and `refresh-index`; never edited by hand outside those runs. **Read this first, always** —
> a question is answered by walking the index and the links before opening any page or note.

*(empty — nothing read yet. Drop a source into `Sources/` and run `read`.)*
