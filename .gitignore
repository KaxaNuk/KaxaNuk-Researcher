# APM: what `apm install` writes, per machine
.agents/
.claude/
.codex/
.cursor/
.gemini/
.opencode/
.windsurf/
apm_modules/
apm.lock.yaml
.mcp.json
GEMINI.md

# Python
__pycache__/
.venv/
.pytest_cache/
.ruff_cache/

# IDE and OS
.idea/
.vscode/
.DS_Store
Thumbs.db

# Python: the package has no lock of its own; every check runs with --no-project
/uv.lock

# Eval runs: the assembled plugin and the harness results.
evals/.run/
evals/results/
