# Log

> Append-only record of every `/compile`, `/audit` and `/refresh-index`, oldest first. Written by
> the researcher, never edited by hand. File-level actions on `Library/` only — never query content,
> never answers, never any path under `Notes/Private/`. The last five entries:
> `grep "^## \[" Library/LOG.md | tail -5`

---
