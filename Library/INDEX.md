# Index

> The one index of everything in `Library/`, grouped by domain, one line per article: a standard
> markdown link and the sentence that says what the article settles. Rebuilt by `/compile` and
> `/refresh-index`; never edited by hand outside those runs. **Read this first, always** — a question
> is answered by walking the index and the links before opening any article.

*(empty — nothing compiled yet. Drop a source into `Sources/` and run `/compile`.)*
